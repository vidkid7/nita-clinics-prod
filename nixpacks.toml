[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm ci --include=dev"]

[phases.build]
cmds = ["cd backend && npm run build && npm prune --omit=dev"]

[start]
cmd = "cd backend && node dist/main"
